2025. 12. 08.
src\engines\analysis\evalute.py

타이밍에 대해 평가할 수 있는 스레드 제작에 들어갔다.
librosa를 써보니 매 블록을 처리하기에 병목이 심해 제대로 동작하지 않는다.
numpy를 통해 간단히 energy 판정을 얻어보자.
오늘은 3초간 녹음을 가지고 threshold를 얻은 뒤에 그보다 10배 큰 소리가 들어오면 attack이라고 판정하도록 했다.
너무 시끄러운 환경(threshold >= 0.1)인 경우에 취약하고, 어택타이밍은 threshold를 넘은 샘플 중 최고점을 얻도록 해야할 것 같다.

다음 작업은 취약점 해결, 정확한 어택타이밍 산출, 정박 타이밍과의 시간거리 계산을 구현하는 것이다.